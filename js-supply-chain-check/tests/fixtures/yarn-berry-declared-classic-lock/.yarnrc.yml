# Looks fully hardened — but yarn.lock is a classic v1 lockfile, so yarn 1.x
# (which ignores .yarnrc.yml) actually produced the tree and none of these are
# enforced at install time.
npmMinimalAgeGate: 10080
approvedGitRepositories: []
enableScripts: false
